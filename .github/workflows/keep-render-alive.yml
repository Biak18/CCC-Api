name: Keep Render API Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render API
        run: curl -fsS https://ccc-api-ohwh.onrender.com/health
